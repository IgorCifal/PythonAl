[build]
  # ESTA LINHA É CRÍTICA: Diz ao Netlify para publicar a raiz (o ponto)
  publish = "." 
  command = "echo 'Iniciando deployment...'"

[functions]
  directory = "netlify-functions"

[build.environment]
  AWS_LAMBDA_PYTHON_RUNTIME = "python3.8"
  PIP_REQUIREMENTS = "requirements.txt"
  # As variáveis secretas FULLTRACK devem estar no PAINEL!